import { StatusBar } from 'expo-status-bar';
import { StyleSheet, Text, View } from 'react-native';

export default function Player() {
  return (
    <View>
      <View>Nguoi chouw</View>
      <StatusBar style="auto" />
    </View>
  );
}